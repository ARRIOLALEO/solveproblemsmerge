export interface Candidate {
    id: string,
    email: string
    password: string
    firstName: string
    lastName: string
    currentTitle: string
    desiredTitle: string
    location: string
    applicationStatus: string
}
